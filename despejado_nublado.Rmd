---
title: "Clasificador metereológico"
output: html_document
date: "2025-12-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
rm(list = ls())
library(jpeg, quietly = T)
library(class, quietly = T)
library(caret, quietly = T)
library(randomForest, quietly = T)
library(magick, quietly = T)
library(tidyr, quietly = T)
library(plotly, quietly = T)
library(ggplot2, quietly = T)
library(GGally, quietly = T)
library(corrplot)
library(patchwork)
# cargamos las funciones de lectura y extracción de características de imágenes
source("funciones_extraccion_caracteristicas.R")
```


# Lectura de las imágenes

Vamos a diferenciar los días despejados de aquellos nublados.
Para ello cargamos todas las imágenes destinadas a entrenamiento y reservaremos un 20% para validación.

```{r}
# Definir la ruta base
ruta_base_imagenes <- "imagenes_recortadas/train/Meteorológico"

# lectura de las imágenes de Nublado
ruta_nublado <- file.path(ruta_base_imagenes, "Nublado")
archivos_nublado <- list.files(
  path = ruta_nublado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado <- data.frame(
  ruta = archivos_nublado,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado <- file.path(ruta_base_imagenes, "Despejado")
archivos_despejado <- list.files(
  path = ruta_despejado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado <- data.frame(
  ruta = archivos_despejado,
  etiqueta = 1 # despejado
)

# lectura de las imágenes 

datos_imagenes <- rbind(df_nublado, df_despejado)

set.seed(42) 

indices_entrenamiento <- createDataPartition(
  y = datos_imagenes$etiqueta,
  p = 0.8,
  list = FALSE 
)

# Crear los conjuntos de datos de TRAIN y VALIDATION
datos_train <- datos_imagenes[indices_entrenamiento, ]
datos_validation <- datos_imagenes[-indices_entrenamiento, ]
```

## Extracción y análisis de características

Para entrenar este nuevo clasificador, que tendrá que distinguir entre cielos nublados y despejados, hemos propuesto un total de 11 características: los valores de los tres canales RGB originales de la imagen, los valores de HSV; descomponiendo el matiz o _hue_ (H) en sus valores de seno y coseno (ya que al ser H un ángulo no es conveniente tratarlo igual que las variables S y V), el brillo, el contraste, el ratio de píxeles azules y la magnitud mediana del operador Sobel (para detectar los bordes de las nubes).

```{r}
# leemos las imágenes
lista_train <- leer_imagenes(datos_train)
lista_validation <- leer_imagenes(datos_validation)

# extraemos las medianas de R, G, y B 
datos_train <- extraer_rgb(datos_train, lista_train)
datos_validation <- extraer_rgb(datos_validation, lista_validation)

# extraemos las medianas del espacio HSV
datos_train <- extraer_hsv(datos_train, lista_train)
datos_validation <- extraer_hsv(datos_validation, lista_validation)

# extraemos las ratios de azul 
datos_train <- extraer_ratio_azul(datos_train, lista_train)
datos_validation <- extraer_ratio_azul(datos_validation, lista_validation)

# extraemos las medianas de brillo y contraste 
datos_train <- extraer_brillo_contraste(datos_train, lista_train)
datos_validation <- extraer_brillo_contraste(datos_validation, lista_validation)

# extraemos los bordes
datos_train <- extraer_sobel(datos_train, lista_train)
datos_validation <- extraer_sobel(datos_validation, lista_validation)
```

```{r, fig.width=15, fig.height=7}
# Transformar datos a formato largo para representarlos 
datos_long <- pivot_longer(datos_train, cols = c("R_mediana", "G_mediana", "B_mediana"), 
                           names_to = "Feature", values_to = "Valor")

# Representamos los histogramas de las variables
f <- ggplot(datos_long, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
g <- ggplot(datos_long, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  labs(title = "Separación de clases usando las medianas de los canales RGB",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()

f+g

ggsave("RGB_despejado_nublado.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)
```

Al representar los histogramas de los valores medianos del espacio RGB, vemos que en los 3 casos podríamos considerar que los datos siguen una distribución normal. Si bien es llamativa la alta concentración del valor 0 en el canal R, esto podría deberse simplemente a la ausencia de este color en imágenes del cielo despejado y nublado. También es destacable que, como era de esperar, el canal con valores medianos más altos es el B y también es este canal el que presenta una diferencia entre la distribución de los datos según las clases más apreciable. Una vez comprobada la distribución que siguen nuestros datos, al realizar un diagrama de cajas podemos observar que, efectivamente, el canal B es el que proporciona una distinción mayor entre clases, aunque esta diste de ser completa. 

```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~R_mediana,
  y = ~G_mediana,
  z = ~B_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'R Mediana'),
    yaxis = list(title = 'G Mediana'),
    zaxis = list(title = 'B Mediana')
  ),
  title = 'Distribución de imágenes por valor mediano de los canales RGB'
)
fig_hsv
```
Representando conjuntamente las tres variables en el espacio, podemos observar que, aunque ambas clases se agrupen consistentemente, la frontera de decisión no está claramente definida.


```{r}
# Transformar datos a formato largo para representarlos 
datos_long_hsv <- pivot_longer(
  datos_train, 
  cols = c("H_cos_mediana", "H_sin_mediana", "S_mediana", "V_mediana"), 
  names_to = "Feature", 
  values_to = "Valor"
  )

# Representamos los histogramas de las variables
f<-ggplot(datos_long_hsv, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
g<-ggplot(datos_long_hsv, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  labs(title = "Separación de clases usando HSV",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
ggsave("HSV_despejado_nublado.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)
```
Al representar los histogramas de los 4 valores extraídos de la representación de las imágenes en el espacio HSV, podemos observar que todas, excepto $\cos H$ parecen presentar una distribución normal.
En el diagrama de cajas podemos observar que la mayor distancia entre clases la presenta el canal S.


```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_cos_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'cos(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv

# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_sin_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'sin(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv
```

Si representamos en el espacio las variables $\cos H$, S,V y después las variables $\sin H$, S,V observaremos que el coseno de la variable H junto con los canales S y V parecen distinguir mejor entre clases que la representación que sustituye el coseno por el seno.


```{r}
# Representamos la relación entre las medianas del cos(H) y sin(H)
ggplot(datos_train, aes(x = H_cos_mediana, y = H_sin_mediana, color = etiqueta)) + 
  geom_jitter(size = 2, alpha = 0.3, width = 0.05, height = 0.05) +
  scale_color_manual(values = c("grey", "skyblue"), 
                     labels = c("Nublado", "Despejado")) + 
  theme_minimal() +
  labs(title = "Relación entre las medianas del coseno y seno del canal H",
       x = "cos(H)",
       y = "sin(H)",
       color = "Clase") +
  coord_fixed()
```
Por último, hemos representado la relación entre las medianas del coseno y seno de los valores del canal H, donde vemos que, la mayoría de valores se agrupan en torno a valores de H entre $\pi$ y $\frac{3}{2}\pi$, sobre todo los puntos de la clase del cielo despejado. Esto es bastante coherente, ya que, sobre todo las imágenes de cielos despejados, contendrán en su mayoría colores azules, los cuales se representan con valores de hue dentro del intervalo 

```{r}
# Transformar datos a formato largo para representarlos 
datos_long_bcr <- pivot_longer(
  datos_train, 
  cols = c( "Ratio_Azul", "Brillo", "Contraste", "Sobel_mean"), 
  names_to = "Feature", values_to = "Valor"
  )

# Representamos los histogramas de las variables
f<-ggplot(datos_long_bcr, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "free") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
g<-ggplot(datos_train, aes(x = Brillo, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  labs(title = "Separación de clases usando el brillo",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal() 

ggsave("bcr_despejado_nublado.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)
```
Como podemos ver en los histogramas, la variable brillo presenta una marcada distribución normal, mientras que las variables contraste y ratio de azul distan mucho de hacerlo, sobre todo el ratio de azul. Es por esto que solo hemos representado el brillo en un diagrama de cajas, que nos demuestra que la variable brillo no es capaz de distinguir claramente entre clases, ya que las medianas de ambas poblaciones están muy próximas y el IQR es bastante similar, lo que denota que el brillo entre imágenes de cielos despejados y cielos nublados es bastante similar. De forma contraria, y aunque no siga una distribución gaussiana, podemos ver en su histograma como el porcentaje de píxeles azules en la imagen distingue claramente entre cielos despejados y nublados, teniendo la mayoría de imágenes de cielos despejados ratios muy altos (cerca del 100%), mientras que la mayoría de cielos nublados tiene ratios muy bajos (cerca del 0%).

Aunque la distinción no sea clara, en el histograma de la media de la magnitud del operador de Sobel podemos ver que las imágenes de cielos despejados tienden a presentar valores más bajos que aquellos que presentan las imágenes de cielos nublados. 

```{r}
# representamos las correlaciones
cor_matrix<-abs(cor(datos_train[, 3:13]))
#diag(cor_matrix)<-0
png("corrplot.png", 
       width = 15, 
       height = 7, 
       units = "in", 
       res = 300)
corrplot(cor_matrix, method="circle", type="upper", tl.col = "black")
dev.off()
```




```{r, fig.width=15, fig.height=15, fig.align='center', dev='png', dpi=300}
# Mostramos las relaciones entre todas las variables originales
p <- ggpairs(datos_train, 
        columns = 3:13, 
        mapping = aes(color = etiqueta, alpha = 0.5),
        upper = list(continuous = wrap("cor", size = 3)), 
        lower = list(continuous = wrap("points", size = 0.5))) +
  scale_color_manual(values = c("gray", "skyblue"), labels = c("Nublado", "Despejado")) +
  scale_fill_manual(values = c("gray",  "skyblue"), labels = c("Nublado", "Despejado")) +
  theme_minimal() +
  theme(axis.text = element_text(size = 7), 
        strip.text = element_text(size = 8)) 

# Mostrar el gráfico
p

# guardamos la imagen
ggsave("mi_grafica_ggpairs.png", 
       plot = p, 
       width = 15, 
       height = 15, 
       units = "in", 
       dpi = 300)
```

## Aplicación de PCA y selección del modelo


```{r}
set.seed(42)
modelo <- randomForest(
  etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones <- predict(modelo, newdata = datos_validation)
confusionMatrix(predicciones, datos_validation$etiqueta)
modelo$importance
```


```{r}
mal_clasificados <- datos_validation[predicciones != datos_validation$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = 'white')
}
```



```{r}
# Calculamos el PCA centrando y escalando los datos
pca <- prcomp(
  ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean,
  data=datos_train, 
  center=TRUE, 
  scale=TRUE
  )
```

```{r}
# Creamos el conjunto de entrenamiento formado por TODAS las componentes principales
datos_pca_train <- data.frame(
  etiqueta = datos_train$etiqueta, 
  pca$x
  ) 

# Calculamos el modelo
set.seed(42)
modelo_pca <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7 + PC8 + PC9 + PC10 + PC11,
  data = datos_pca_train,
  ntree = 100
)

# Calculamos el conjunto de validación
datos_pca_validacion <- predict(pca, newdata = datos_validation) 
datos_pca_validacion <- data.frame(
  etiqueta = datos_validation$etiqueta,
  ruta = datos_validation$ruta,
  datos_pca_validacion
) 

# Evaluar el modelo en el conjunto de prueba
predicciones_pca <- predict(modelo_pca, newdata = datos_pca_validacion)
confusionMatrix(predicciones_pca, datos_pca_validacion$etiqueta)
modelo_pca$importance
```


```{r}

# Calculamos la proporción de varianza explicada y las proporciones acumuladas
prop_varianza <- pca$sdev^2 / sum(pca$sdev^2)
prop_varianza_acum <- cumsum(prop_varianza)

# Representamos la proporción de la varianza explicada
f<-ggplot(data = data.frame(prop_varianza, pc = 1:11),
aes(x = pc, y = prop_varianza)) +
  geom_col(width = 0.3, color='skyblue', fill = 'skyblue') +
  scale_y_continuous(limits = c(0,1)) +
  theme_bw()+
  labs(x = "Componentes principales",
       y = "Proporción de varianza explicada")

# Representamos la proporción de varianza explicada acumulada
g<-ggplot(data = data.frame(prop_varianza_acum, pc = 1:11),
       aes(x = pc, y = prop_varianza_acum, group = 1)) +
  geom_point(color='skyblue') +
  geom_line(color = 'skyblue') +
  theme_bw() +
  labs(x = "Número de componentes principales",
       y = "Proporción de varianza explicada acumulada")

ggsave("varianza.png", 
       plot = f, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

ggsave("varianza_acumulada.png", 
       plot = g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

ggsave("pca_varianzas.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

f+g
```

En la curva de proporción de varianza acumulada vemos que, a partir de la sexta componente principal, la proporción de varianza que explica cada una de las siguientes componentes es casi imperceptible. Es por esto por lo que escogeremos solamente las 7 primeras componentes principales para entrenar otro modelo de Random Forest, con la intención de simplificar el modelo para evitar sobreajustarnos a los datos de entrenamiento.

```{r}
# Creamos el conjunto de entrenamiento formado por las 7 primeras componentes principales
datos_pca_train <- data.frame(
  etiqueta = datos_train$etiqueta, 
  pca$x
  ) %>% select("etiqueta", paste0("PC",1:7))

# Calculamos el modelo
set.seed(42)
modelo_pca <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7,
  data = datos_pca_train,
  ntree = 100
)

# Calculamos el conjunto de validación
datos_pca_validacion <- predict(pca, newdata = datos_validation) 
datos_pca_validacion <- data.frame(
  etiqueta = datos_validation$etiqueta,
  ruta = datos_validation$ruta,
  datos_pca_validacion
) %>% select("etiqueta", "ruta", paste0("PC",1:7))

# Evaluar el modelo en el conjunto de prueba
predicciones_pca <- predict(modelo_pca, newdata = datos_pca_validacion)
confusionMatrix(predicciones_pca, datos_pca_validacion$etiqueta)
modelo_pca$importance
```


Mostramos las imágenes mal clasificadas:

```{r}
mal_clasificados <- datos_pca_validacion[predicciones_pca != datos_pca_validacion$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = '#a44')
}
```
```{r}
# # Correlaciones entre las 6 primeras componentes principales
# p <- ggpairs(datos_pca_train, 
#         columns = 2:7, 
#         mapping = aes(color = etiqueta, alpha = 0.5),
#         upper = list(continuous = wrap("cor", size = 3)), 
#         lower = list(continuous = wrap("points", size = 0.5))) + 
#   scale_color_manual(values = c("gray", "skyblue"), labels = c("Nublado", "Despejado")) +
#   scale_fill_manual(values = c("gray",  "skyblue"), labels = c("Nublado", "Despejado")) +
#   theme_minimal() +
#   theme(axis.text = element_text(size = 7), 
#         strip.text = element_text(size = 8)) 
# 
# # Mostrar el gráfico
# p
# 
# ggsave("mi_grafica_ggpairs_pca.png", 
#        plot = p, 
#        width = 15, 
#        height = 15, 
#        units = "in", 
#        dpi = 300)
```


# Probamos con filtro paso alto antes de aplicar Sobel para detectar mejor bordes

En las imagenes mal clasificadas, hemos visto que es posible que no este identificando bien los bordes, al tratarse de cielo muy cubiertos con nubes muy homogéneas. Probamos a añadir un filtro paso alto antes de aplicar Sobel para ver si mejora la clasificación.

```{r}
library(imager)

mejorar_texturas <- function(df, lista, sigma = 1, potencia = 0.8) {
  # Creamos una lista vacía para las nuevas imágenes
  lista_procesada <- list()
  
  cat("Aplicando filtro de realce de bordes a", nrow(df), "imágenes...\n")
  
  for (nombre in df$ruta) {
    # 1. Convertimos el array de la lista a objeto de imagen (cimg)
    # Asumimos que la imagen está en formato [filas, columnas, canales]
    img <- suppressWarnings(as.cimg(img_array))
    
    # 2. Creamos una versión de Paso Bajo (suavizada/blur)
    # sigma controla el radio del filtro (1 o 2 es lo ideal)
    img_low_pass <- isoblur(img, sigma = sigma)
    
    # 3. Obtenemos el Paso Alto (Original - Paso Bajo)
    # Esto contiene solo bordes, texturas y ruido
    img_high_pass <- img - img_low_pass
    
    # 4. Sumamos el Paso Alto a la Original para realzar (Sharpen)
    # potencia controla cuánto se marcan los bordes
    img_final <- img + (potencia * img_high_pass)
    
    # 5. Aseguramos que los valores sigan entre 0 y 1 (clipping)
    img_final[img_final < 0] <- 0
    img_final[img_final > 1] <- 1
    
    # Guardamos en la nueva lista volviendo a convertir a array si es necesario
    lista_procesada[[nombre]] <- as.array(img_final)
  }
  
  cat("Proceso completado.\n")
  return(lista_procesada)
}

extraer_sobel2 <- function(df, lista) {
  features <- numeric(nrow(df))
  cat("Extrayendo Sobel de", nrow(df), "imágenes...\n")
  
  j <- 1
  for (nombre in df$ruta) {
    img_array <- lista[[nombre]]
    
    # conversión a formato cimg
    img_cimg <- suppressWarnings(as.cimg(img_array))
    # escala de grises
    img_gray <- grayscale(img_array)
    # cálculo de gradientes
    gradientes <- imgradient(img_gray, "xy")
    # combinamos ambos gradientes
    magnitud <- sqrt(gradientes$x^2 + gradientes$y^2)
    
    features[j] <- mean(magnitud, na.rm = TRUE)
    j <- j + 1
  }
  
  df_final <- cbind(df, Sobel_2 = features)
  return(df_final)
}

lista_procesada_train <- mejorar_texturas(datos_train, lista_train)
lista_procesada_validation <- mejorar_texturas(datos_validation, lista_validation)
lista_procesada_test <- mejorar_texturas(datos_test, lista_test)

datos_train <- extraer_sobel2(datos_train, lista_procesada_train)
datos_validation <- extraer_sobel2(datos_validation, lista_procesada_validation)
datos_test <- extraer_sobel2(datos_test, lista_procesada_test)
```



### Test con nuestras fotos

Cargamos, leemos y etiquetamos los datos: 

```{r}
# Definir la ruta base
ruta_base_imagenes <- "imagenes_recortadas/test/Meteorológico"

# lectura de las imágenes de Nublado
ruta_nublado <- file.path(ruta_base_imagenes, "Nublado")
archivos_nublado <- list.files(
  path = ruta_nublado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado <- data.frame(
  ruta = archivos_nublado,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado <- file.path(ruta_base_imagenes, "Despejado")
archivos_despejado <- list.files(
  path = ruta_despejado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado <- data.frame(
  ruta = archivos_despejado,
  etiqueta = 1 # despejado
)

# lectura de las imágenes 
datos_test <- rbind(df_nublado, df_despejado)
lista_test <- leer_imagenes(datos_test)

# extracción de características
datos_test <- datos_test %>% 
  extraer_rgb(lista_test) %>% 
  extraer_hsv(lista_test) %>% 
  extraer_brillo_contraste(lista_test) %>% 
  extraer_ratio_azul(lista_test) %>% 
  extraer_sobel(lista_test)
```
Cogemos las columnas de esas 11 variables (ahora tenemos mas porque hemos ejecutado cosas posteriores, de ahi filtrar)
```{r}
datos_test_11 <- datos_test %>% select(
  ruta, 
  etiqueta, 
  R_mediana, 
  G_mediana, 
  B_mediana, 
  H_cos_mediana, 
  H_sin_mediana, 
  S_mediana, 
  V_mediana, 
  Ratio_Azul, 
  Brillo, 
  Contraste, 
  Sobel_mean
  )

datos_entrenamiento_11 <- datos_entrenamiento %>% select(
  ruta, 
  etiqueta, 
  R_mediana, 
  G_mediana, 
  B_mediana, 
  H_cos_mediana, 
  H_sin_mediana, 
  S_mediana, 
  V_mediana, 
  Ratio_Azul, 
  Brillo, 
  Contraste, 
  Sobel_mean
  )



```


Aplicamos el modelo:

```{r}
modelo <- randomForest(
  etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean,
  data = datos_train,
  ntree = 100
)

# Evaluar el modelo en el conjunto de validacion
predicciones <- predict(modelo, newdata = datos_validation)
confusionMatrix(predicciones, datos_validation$etiqueta)
modelo$importance
mal_clasificados <- datos_validation[predicciones!= datos_validation$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = '#a44')
}
```
Probamos a hacer pca con esas 11 variables: 
```{r}

pca2 <- prcomp(
  ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean,
  data=datos_entrenamiento, 
  center=TRUE, 
  scale=TRUE
  )

```

Probamos a hacer pca y coger todas las componentes:

```{r}
# Creamos el conjunto de entrenamiento con todas las componentes principales
datos_pca2_train <- data.frame(
  etiqueta = datos_entrenamiento$etiqueta, 
  pca2$x
  ) 

```

```{r}
# Calculamos la proporción de varianza explicada y las proporciones acumuladas
prop_varianza <- pca2$sdev^2 / sum(pca2$sdev^2)
prop_varianza_acum <- cumsum(prop_varianza)

# Representamos la proporción de varianza explicada acumulada
ggplot(data = data.frame(prop_varianza_acum, pc = 1:11),
       aes(x = pc, y = prop_varianza_acum, group = 1)) +
  geom_point(color='skyblue') +
  geom_line(color = 'skyblue') +
  theme_bw() +
  labs(x = "Número de componentes principales",
       y = "Proporción de varianza explicada acumulada")
```



```{r}

# Calculamos el modelo
set.seed(42)
modelo_pca2 <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7 + PC8 + PC9 + PC10 + PC11,
  data = datos_pca2_train,
  ntree = 100
)

# Calculamos el conjunto de test
datos_pca_test <- predict(pca2, newdata = datos_test) 
datos_pca_test <- data.frame(
  etiqueta = datos_test$etiqueta,
  ruta = datos_test$ruta,
  datos_pca_test
) 

# Evaluar el modelo en el conjunto de prueba
predicciones_pca2 <- predict(modelo_pca2, newdata = datos_pca_test)
confusionMatrix(predicciones_pca2, datos_pca_test$etiqueta)
modelo_pca2$importance
```
```{r}

# Creamos el conjunto de entrenamiento formado por las 7 primeras componentes principales
datos_pca2_train_7 <- data.frame(
  etiqueta = datos_entrenamiento$etiqueta, 
  pca2$x
  ) %>% select("etiqueta", paste0("PC",1:7))

# Calculamos el modelo
set.seed(42)
modelo_pca2 <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7,
  data = datos_pca2_train_6,
  ntree = 100
)

# Calculamos el conjunto de test
datos_pca_test <- predict(pca2, newdata = datos_test) 
datos_pca_test_6 <- data.frame(
  etiqueta = datos_test$etiqueta,
  ruta = datos_test$ruta,
  datos_pca_test
) %>% select("etiqueta", "ruta", paste0("PC",1:7))

# Evaluar el modelo en el conjunto de prueba
predicciones_pca2 <- predict(modelo_pca2, newdata = datos_pca_test_6)
confusionMatrix(predicciones_pca2, datos_pca_test_6$etiqueta)
modelo_pca2$importance
```

```{r}
mal_clasificados <- datos_pca_test[predicciones_pca2!= datos_pca_test$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = '#a44')
}
```


## Probamos cositas nuevas:


```{r}
extraer_saturacion_bloques <- function(df, lista, num_bloques = 4) {
  
  # lista para guardar los vectores de características de cada imagen
  features_list <- list()
  
  cat("Extrayendo saturación por bloques de", nrow(df), "imágenes...\n")
  
  for (j in 1:nrow(df)) {
    nombre <- df$ruta[j]
    
    # dimensiones originales
    altura <- nrow(lista[[nombre]][,,1])
    ancho <- ncol(lista[[nombre]][,,1])
    
    # extraer canales HSV
    hsv_vals <- rgb_a_hsv(
      as.vector(lista[[nombre]][,,1]), 
      as.vector(lista[[nombre]][,,2]), 
      as.vector(lista[[nombre]][,,3])
    )
    
    # reconstruimos solo la matriz de Saturación (canal 2)
    # cada valor corresponde a un píxel y la estructura es como la imagen
    S_matrix <- matrix(hsv_vals[, 2], nrow = altura, ncol = ancho)
    
    # calculamos el tamaño de cada bloque
    bloque_altura <- floor(altura / num_bloques)
    bloque_ancho <- floor(ancho / num_bloques)
    
    # vector para las características de la imagen actual
    caracteristicas_imagen <- c()
    
    # doble bucle para recorrer la cuadrícula
    for (i in 0:(num_bloques - 1)) {
      for (k in 0:(num_bloques - 1)) {
        
        # índices de inicio y fin del bloque
        fila_inicio <- i * bloque_altura + 1
        fila_fin <- ifelse(i == num_bloques - 1, altura, (i + 1) * bloque_altura)
        col_inicio <- k * bloque_ancho + 1
        col_fin <- ifelse(k == num_bloques - 1, ancho, (k + 1) * bloque_ancho)
        
        # extraemos el bloque de la matriz de saturación
        bloque_S <- S_matrix[fila_inicio:fila_fin, col_inicio:col_fin]
        
        # cálculo de la saturación media del bloque
        # Un cielo despejado tendrá valores altos (>0.3), uno nublado valores bajos (<0.15)
        media_S <- mean(bloque_S, na.rm = TRUE)
        
        # acumulamos en el vector de la imagen
        caracteristicas_imagen <- c(caracteristicas_imagen, media_S)
      }
    }
    features_list[[j]] <- caracteristicas_imagen
  }
  
# convertimos la lista a matriz
  features_matrix <- do.call(rbind, features_list)
  
  # asignamos nombres a las columnas 
  colnames(features_matrix) <- paste0("Bloque_", rep(1:(num_bloques^2)), "_media_S")
  
  # unimos la nueva info al dataframe y nos aseguramos de que la etiqueta es 
  # de tipo factor
  df_final <- cbind(df, features_matrix)
  # devolvemos el dataframe original con los nuevos datos añadidos
  df_final$etiqueta <- as.factor(df_final$etiqueta)
  
  return(df_final)
}

extraer_entropia <- function(df, lista) {
  
  # Inicializamos el vector para la entropía
  features <- matrix(0, nrow = nrow(df), ncol = 1)
  
  cat("Extrayendo características de Entropía de", nrow(df), "imágenes...\n")
  
  j <- 1
  for (nombre in df$ruta) {
    img_array <- lista[[nombre]]
    
    # conversión a grises
    gris_matrix <- 0.299 * img_array[,,1] + 0.587 * img_array[,,2] + 0.114 * img_array[,,3]
    tabla_frecuencias <- table(as.vector(round(gris_matrix, 3))) 
    # cálculo de la entropía
    features[j] <- entropy(tabla_frecuencias)
    
    j <- j + 1
  }
  
  df_final <- cbind(df, Entropia = features)
  
  if("etiqueta" %in% names(df_final)) {
    df_final$etiqueta <- as.factor(df_final$etiqueta)
  }
  return(df_final)
}

extraer_ratio_blanco <- function(df, lista) {
  features <- numeric(nrow(df))
  cat("Extrayendo ratios de blanco de", nrow(df), "imágenes...\n")
  
  j <- 1
  for (nombre in df$ruta) {
    # Convertimos a HSV
    hsv_vals <- rgb_a_hsv(
      as.vector(lista[[nombre]][,,1]), 
      as.vector(lista[[nombre]][,,2]), 
      as.vector(lista[[nombre]][,,3])
    )
    
    S <- hsv_vals[, 2] # Saturación
    V <- hsv_vals[, 3] # Valor (Brillo)
    
    total_pixeles <- length(S)
    
    # Filtro: Poca saturación y mucho brillo
    pixeles_blancos <- sum(S <= 0.15 & V >= 0.85)
    
    features[j] <- (pixeles_blancos / total_pixeles)
    j <- j + 1
  }
  
  df_final <- cbind(df, Ratio_Blanco = features)
  df_final$etiqueta <- as.factor(df_final$etiqueta)
  return(df_final)
}

extraer_ratio_gris <- function(df, lista) {
  features <- numeric(nrow(df))
  cat("Extrayendo ratios de gris de", nrow(df), "imágenes...\n")
  
  j <- 1
  for (nombre in df$ruta) {
    hsv_vals <- rgb_a_hsv(
      as.vector(lista[[nombre]][,,1]), 
      as.vector(lista[[nombre]][,,2]), 
      as.vector(lista[[nombre]][,,3])
    )
    
    S <- hsv_vals[, 2]
    V <- hsv_vals[, 3]
    
    total_pixeles <- length(S)
    
    # Filtro: Saturación casi nula y brillo intermedio
    # Ajustamos V entre 0.2 y 0.85 para evitar negros y blancos puros
    pixeles_grises <- sum(S <= 0.15 & V > 0.15 & V < 0.85)
    
    features[j] <- (pixeles_grises / total_pixeles)
    j <- j + 1
  }
  
  df_final <- cbind(df, Ratio_Gris = features)
  df_final$etiqueta <- as.factor(df_final$etiqueta)
  return(df_final)
}


extraer_ratio_sombras <- function(df, lista) {
  # vector vacío para guardar los ratios
  features <- numeric(nrow(df))
  
  cat("Extrayendo ratios de sombras de", nrow(df), "imágenes...\n")
  
  j <- 1
  for (nombre in df$ruta) {
    
    # Calculamos los valores HSV 
    # (Asumiendo que rgb_a_hsv devuelve una matriz con H, S, V)
    hsv_vals <- rgb_a_hsv(
      as.vector(lista[[nombre]][,,1]), # R
      as.vector(lista[[nombre]][,,2]), # G
      as.vector(lista[[nombre]][,,3])  # B
    )
    
    # El canal V (Value/Brightness) es el 3º
    V <- hsv_vals[, 3]
    
    # número total de píxeles
    total_pixeles <- length(V)
    
    # Filtramos por píxeles muy oscuros (sombras)
    # Umbral sugerido: V < 0.2 (20% de brillo)
    pixeles_sombras <- sum(V < 0.2)
    
    # Porcentaje de sombras
    features[j] <- (pixeles_sombras / total_pixeles)
    
    j <- j + 1
  }
  
  # Unimos la info al dataframe
  df_final <- cbind(df, Ratio_Sombras = features)
  df_final$etiqueta <- as.factor(df_final$etiqueta)
  
  return(df_final)
}
```

```{r}
datos_train <- extraer_saturacion_bloques(datos_train, lista_train) 
datos_train <- extraer_entropia(datos_train, lista_train)
datos_train <- extraer_ratio_blanco(datos_train, lista_train)
datos_train <- extraer_ratio_gris(datos_train, lista_train)
datos_train <- extraer_sd_V(datos_train, lista_train)
datos_train <- extraer_ratio_sombras(datos_train, lista_train)

datos_validation <- extraer_saturacion_bloques(datos_validation, lista_validation) 
datos_validation <- extraer_entropia(datos_validation, lista_validation)
datos_validation <- extraer_ratio_blanco(datos_validation, lista_validation)
datos_validation <- extraer_ratio_gris(datos_validation, lista_validation)
datos_validation <- extraer_sd_V(datos_validation, lista_validation)
datos_validation <- extraer_ratio_sombras(datos_validation, lista_validation)

datos_entrenamiento <- rbind(datos_train,datos_validation)

datos_test <- extraer_saturacion_bloques(datos_test, lista_test) 
datos_test <- extraer_entropia(datos_test, lista_test)
datos_test <- extraer_ratio_blanco(datos_test, lista_test)
datos_test <- extraer_ratio_gris(datos_test, lista_test)
datos_test <- extraer_sd_V(datos_test, lista_test)
datos_test <- extraer_ratio_sombras(datos_test, lista_test)
```


```{r}
# MODELO CON VALIDACION
set.seed(42)
modelo <- randomForest(
  etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean + Entropia + Ratio_Blanco + Ratio_Gris + sd_V + Ratio_Sombras,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones <- predict(modelo, newdata = datos_validation)
confusionMatrix(predicciones, datos_validation$etiqueta)
modelo$importance


# MODELO CON TEST
datos_entrenamiento <- rbind(datos_train, datos_validation)
pca2 <- prcomp(
  ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean + Entropia + Ratio_Blanco + Ratio_Gris + sd_V+ Ratio_Sombras,
  data=datos_entrenamiento, 
  center=TRUE, 
  scale=TRUE
  )

modelo <- randomForest(
  etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean + Entropia + Ratio_Blanco + Ratio_Gris + sd_V+ Ratio_Sombras,
  data = datos_entrenamiento,
  ntree = 100
)

# Evaluar el modelo en el conjunto de prueba
predicciones <- predict(modelo, newdata = datos_test)
confusionMatrix(predicciones, datos_test$etiqueta)
modelo$importance
```


```{r}
prop_varianza <- pca2$sdev^2 / sum(pca2$sdev^2)
prop_varianza_acum <- cumsum(prop_varianza)

# Representamos la proporción de la varianza explicada
f<-ggplot(data = data.frame(prop_varianza, pc = 1:15),
aes(x = pc, y = prop_varianza)) +
  geom_col(width = 0.3, color='skyblue', fill = 'skyblue') +
  scale_y_continuous(limits = c(0,1)) +
  theme_bw()+
  labs(x = "Componentes principales",
       y = "Proporción de varianza explicada")

# Representamos la proporción de varianza explicada acumulada
g<-ggplot(data = data.frame(prop_varianza_acum, pc = 1:15),
       aes(x = pc, y = prop_varianza_acum, group = 1)) +
  geom_point(color='skyblue') +
  geom_line(color = 'skyblue') +
  theme_bw() +
  labs(x = "Número de componentes principales",
       y = "Proporción de varianza explicada acumulada")
g
```

# ADRIANA:




# 16 variables sin bloques --> accuracy 0.73

```{r}
library(dplyr)
library(caret) #
library(randomForest)

# --- 1. Preparar los datos eliminando los bloques ---

# Partimos de tus datos completos ya cargados y con características extraídas:
# datos_entrenamiento (train + validation unidos)
# datos_test

# Usamos dplyr::select con un signo menos y 'starts_with' para borrar
# todo lo que empiece por "Bloque_" y también la columna "ruta" que no sirve para entrenar.

datos_train_reducido <- datos_entrenamiento %>%
  select(-starts_with("Bloque_"), -ruta)

datos_test_reducido <- datos_test %>%
  select(-starts_with("Bloque_"), -ruta)

# Verificamos con qué variables nos quedamos
cat("Variables restantes para el modelo:\n")
print(names(datos_train_reducido))


# --- 2. Entrenar Random Forest con el set reducido (SIN PCA) ---

set.seed(42)
# Aumentamos un poco los árboles (ntree=500) para que sea más estable
modelo_reducido <- randomForest(
  etiqueta ~ .,           # Usa todas las variables que quedan
  data = datos_train_reducido,
  ntree = 100,
  importance = TRUE       # Para ver qué variable gana ahora
)


# --- 3. Evaluar con el set de Test ---

# Hacemos las predicciones
predicciones_reducido <- predict(modelo_reducido, newdata = datos_test_reducido)

# Mostramos la matriz de confusión
print("Matriz de Confusión (Modelo Reducido sin Bloques):")
print(confusionMatrix(predicciones_reducido, datos_test_reducido$etiqueta))


# --- 4. Ver la importancia de las variables ---
# ¿Qué características son las reinas ahora que no están los bloques?
varImpPlot(modelo_reducido,
           main = "Importancia de Variables (Sin Bloques Espaciales)")
```



# 16 variables + 16 bloques --> accuracy 0.8
```{r}
# Entrenar con TODAS las variables originales, sin PCA
set.seed(42)
modelo_full <- randomForest(
  etiqueta ~ ., 
  data = select(datos_entrenamiento, -ruta), # Quitamos la ruta, usamos todo lo demás
  ntree = 100,     
  importance = TRUE
)

# Predicción directa
predicciones_full <- predict(modelo_full, newdata = datos_test)
confusionMatrix(predicciones_full, datos_test$etiqueta)

# Y lo más importante: ¿Qué variable manda ahora?
varImpPlot(modelo_full)
```