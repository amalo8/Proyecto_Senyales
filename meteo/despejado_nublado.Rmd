---
title: "Clasificador metereológico"
output: html_document
date: "2025-12-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
rm(list = ls())
library(jpeg, quietly = T)
library(class, quietly = T)
library(caret, quietly = T)
library(randomForest, quietly = T)
library(magick, quietly = T)
library(tidyr, quietly = T)
library(plotly, quietly = T)
library(ggplot2, quietly = T)
library(GGally, quietly = T)
library(corrplot)
library(patchwork)
# cargamos las funciones de lectura y extracción de características de imágenes
source("../funciones_extraccion_caracteristicas.R")
```


# Lectura de las imágenes

Vamos a diferenciar los días despejados de aquellos nublados.
Para ello cargamos todas las imágenes destinadas a entrenamiento y reservaremos un 20% para validación.

```{r}
# Definir la ruta base
ruta_base_imagenes <- "imagenes_recortadas/train/Meteorológico"

# lectura de las imágenes de Nublado
ruta_nublado <- file.path(ruta_base_imagenes, "Nublado")
archivos_nublado <- list.files(
  path = ruta_nublado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado <- data.frame(
  ruta = archivos_nublado,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado <- file.path(ruta_base_imagenes, "Despejado")
archivos_despejado <- list.files(
  path = ruta_despejado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado <- data.frame(
  ruta = archivos_despejado,
  etiqueta = 1 # despejado
)

# lectura de las imágenes 

datos_imagenes <- rbind(df_nublado, df_despejado)

set.seed(42) 

indices_entrenamiento <- createDataPartition(
  y = datos_imagenes$etiqueta,
  p = 0.8,
  list = FALSE 
)

# Crear los conjuntos de datos de TRAIN y VALIDATION
datos_train <- datos_imagenes[indices_entrenamiento, ]
datos_validation <- datos_imagenes[-indices_entrenamiento, ]
```

## Extracción y análisis de características

Para entrenar este nuevo clasificador, que tendrá que distinguir entre cielos nublados y despejados, hemos propuesto un total de 30 características: los valores de los tres canales RGB originales de la imagen, los valores de HSV; descomponiendo el matiz o _hue_ (H) en sus valores de seno y coseno (ya que al ser H un ángulo no es conveniente tratarlo igual que las variables S y V), el brillo, el contraste, la magnitud mediana del operador Sobel (para detectar los bordes de las nubes con mayor precisión hemos aplicado un filtro de paso alto a las imágenes antes de extraer esta característica), los ratios de píxeles azules, blancos, grises, el ratio de sombras, la desviación estándar de V y los valores medios de S por bloques (bloques resultado de dividir la imagen en 16 rectángulos iguales).

```{r}
# leemos las imágenes
lista_train <- leer_imagenes(datos_train)
lista_validation <- leer_imagenes(datos_validation)

# extraemos las medianas de R, G, y B 
datos_train <- extraer_rgb(datos_train, lista_train)
datos_validation <- extraer_rgb(datos_validation, lista_validation)

# extraemos las medianas del espacio HSV
datos_train <- extraer_hsv(datos_train, lista_train)
datos_validation <- extraer_hsv(datos_validation, lista_validation)

# extraemos las medianas de brillo y contraste 
datos_train <- extraer_brillo_contraste(datos_train, lista_train)
datos_validation <- extraer_brillo_contraste(datos_validation, lista_validation)

# aplicamos filtro de paso alto a las imágenes
lista_procesada_train <- mejorar_texturas(datos_train, lista_train)
lista_procesada_validation <- mejorar_texturas(datos_validation, lista_validation)
# extraemos los bordes
datos_train <- extraer_sobel(datos_train, lista_procesada_train)
datos_validation <- extraer_sobel(datos_validation, lista_procesada_validation)

# extraemos las ratios de azul, grises, blancos y sombras 
datos_train <- datos_train %>% 
  extraer_ratio_azul(lista_train) %>% 
  extraer_ratio_blanco(lista_train) %>% 
  extraer_ratio_gris(lista_train) %>% 
  extraer_ratio_sombras(lista_train)
datos_validation <- datos_validation %>% 
  extraer_ratio_azul(lista_validation) %>% 
  extraer_ratio_blanco(lista_validation) %>% 
  extraer_ratio_gris(lista_validation) %>% 
  extraer_ratio_sombras(lista_validation)

# extraemos la desviación estándar de sd_V
datos_train <- extraer_sd_V(datos_train, lista_train)
datos_validation <- extraer_sd_V(datos_validation, lista_validation)

# extraemos S por bloques
datos_train <- extraer_saturacion_bloques(datos_train, lista_train) 
datos_validation <- extraer_saturacion_bloques(datos_validation, lista_validation) 
```

```{r, fig.width=15, fig.height=7}
# Transformar datos a formato largo para representarlos 
datos_long <- pivot_longer(datos_train, cols = c("R_mediana", "G_mediana", "B_mediana"), 
                           names_to = "Feature", values_to = "Valor")

# Representamos los histogramas de las variables
f <- ggplot(datos_long, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
g <- ggplot(datos_long, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  labs(title = "Separación de clases usando las medianas de los canales RGB",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()

f+g

ggsave("../figuras/RGB_despejado_nublado.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)
```

Al representar los histogramas de los valores medianos del espacio RGB, vemos que en los 3 casos podríamos considerar que los datos siguen una distribución normal. Si bien es llamativa la alta concentración del valor 0 en el canal R, esto podría deberse simplemente a la ausencia de este color en imágenes del cielo despejado y nublado. También es destacable que, como era de esperar, el canal con valores medianos más altos es el B y también es este canal el que presenta una diferencia entre la distribución de los datos según las clases más apreciable. Una vez comprobada la distribución que siguen nuestros datos, al realizar un diagrama de cajas podemos observar que, efectivamente, el canal B es el que proporciona una distinción mayor entre clases, aunque esta diste de ser completa. 

```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~R_mediana,
  y = ~G_mediana,
  z = ~B_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'R Mediana'),
    yaxis = list(title = 'G Mediana'),
    zaxis = list(title = 'B Mediana')
  ),
  title = 'Distribución de imágenes por valor mediano de los canales RGB'
)
fig_hsv
```
Representando conjuntamente las tres variables en el espacio, podemos observar que, aunque ambas clases se agrupen consistentemente, la frontera de decisión no está claramente definida.


```{r}
# Transformar datos a formato largo para representarlos 
datos_long_hsv <- pivot_longer(
  datos_train, 
  cols = c("H_cos_mediana", "H_sin_mediana", "S_mediana", "V_mediana"), 
  names_to = "Feature", 
  values_to = "Valor"
  )

# Representamos los histogramas de las variables
f<-ggplot(datos_long_hsv, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
g<-ggplot(datos_long_hsv, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  labs(title = "Separación de clases usando HSV",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
ggsave("../figuras/HSV_despejado_nublado.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)
f+g
```
Al representar los histogramas de los 4 valores extraídos de la representación de las imágenes en el espacio HSV, podemos observar que todas, excepto $\cos H$ parecen presentar una distribución normal.
En el diagrama de cajas podemos observar que la mayor distancia entre clases la presenta el canal S.


```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_cos_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'cos(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv

# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_sin_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'sin(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv
```

Si representamos en el espacio las variables $\cos H$, S,V y después las variables $\sin H$, S,V observaremos que el coseno de la variable H junto con los canales S y V parecen distinguir mejor entre clases que la representación que sustituye el coseno por el seno.


```{r}
# Representamos la relación entre las medianas del cos(H) y sin(H)
ggplot(datos_train, aes(x = H_cos_mediana, y = H_sin_mediana, color = etiqueta)) + 
  geom_jitter(size = 2, alpha = 0.3, width = 0.05, height = 0.05) +
  scale_color_manual(values = c("grey", "skyblue"), 
                     labels = c("Nublado", "Despejado")) + 
  theme_minimal() +
  labs(title = "Relación entre las medianas del coseno y seno del canal H",
       x = "cos(H)",
       y = "sin(H)",
       color = "Clase") +
  coord_fixed()
```
Por último, hemos representado la relación entre las medianas del coseno y seno de los valores del canal H, donde vemos que, la mayoría de valores se agrupan en torno a valores de H entre $\pi$ y $\frac{3}{2}\pi$, sobre todo los puntos de la clase del cielo despejado. Esto es bastante coherente, ya que, sobre todo las imágenes de cielos despejados, contendrán en su mayoría colores azules, los cuales se representan con valores de hue dentro del intervalo 

```{r, fig.width=15, fig.height=5}
# Transformar datos a formato largo para representarlos 
datos_long_bcr <- pivot_longer(
  datos_train, 
  cols = c( "Ratio_Azul", "Brillo", "Contraste", "Sobel_mean","Ratio_Blanco", "Ratio_Gris", "Ratio_Sombras", "sd_V"), 
  names_to = "Feature", values_to = "Valor"
  )

# Representamos los histogramas de las variables
f <- ggplot(datos_long_bcr, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "free", nrow = 2, ncol = 4) +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

ggsave("../figuras/bcr_despejado_nublado.png", 
       plot = f, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

f
```
Como podemos ver en los histogramas, la variable brillo presenta una marcada distribución normal, mientras que las variables contraste y ratio de azul distan mucho de hacerlo, sobre todo el ratio de azul. si nos fijamos en el histograma del brillo, este nos muestra que la variable brillo no es capaz de distinguir claramente entre clases, ya que las medianas de ambas poblaciones están muy próximas, lo que denota que el brillo entre imágenes de cielos despejados y cielos nublados es bastante similar. De forma contraria, y aunque no siga una distribución gaussiana, podemos ver en su histograma como los porcentajes de píxeles azules y los de píxeles grises distinguen claramente entre cielos despejados y nublados, teniendo la mayoría de imágenes de cielos despejados ratios muy altos (cerca de 1), mientras que la mayoría de cielos nublados tiene ratios muy bajos (cerca de 0). Por el contrario, los ratios de píxeles blancos y sombras se solapan casi completamente, lo cual podría estar indicándonos que, independientemente de la clase de procendencia, todas las imágenes tienen valores de saturación y brillo en rangos prácticamente idénticos.

Aunque la distinción no sea clara, en el histograma de la media de la magnitud del operador de Sobel podemos ver que las imágenes de cielos despejados tienden a presentar valores más bajos que aquellos que presentan las imágenes de cielos nublados. 

```{r}
# representamos las correlaciones
cor_matrix<-abs(cor(datos_train[, 3:17]))
#diag(cor_matrix)<-0
png("corrplot.png", 
       width = 15, 
       height = 7, 
       units = "in", 
       res = 300)
corrplot(cor_matrix, method="circle", type="upper", tl.col = "black")
dev.off()
corrplot(cor_matrix, method="circle", type="upper", tl.col = "black")
```


```{r, fig.width=15, fig.height=15}
datos_long_bloques <- pivot_longer(
  datos_train, 
  cols = starts_with("Bloque_"), 
  names_to = "Feature", values_to = "Valor"
  )

# Representamos los histogramas de las variables
f <- ggplot(datos_long_bloques, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Despejado")) +
  facet_wrap(~ Feature, scales = "free", ncol = 4, nrow = 4) +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

ggsave("../figuras/bloques_despejado_nublado.png", 
       plot = f, 
       width = 15, 
       height = 15, 
       units = "in", 
       dpi = 300)

f
```


## Aplicación de PCA y selección del modelo


```{r}
# Calculamos el PCA centrando y escalando los datos
pca <- prcomp(
  ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean + Bloque_1_media_S + Bloque_2_media_S + Bloque_3_media_S + Bloque_4_media_S + Bloque_5_media_S + Bloque_6_media_S +Bloque_7_media_S +Bloque_8_media_S + Bloque_9_media_S +Bloque_10_media_S + Bloque_11_media_S + Bloque_12_media_S + Bloque_13_media_S + Bloque_14_media_S + Bloque_15_media_S + Bloque_16_media_S + Ratio_Blanco + Ratio_Gris + sd_V + Ratio_Sombras,
  data=datos_train, 
  center=TRUE, 
  scale=TRUE
  )
```

```{r}

# Calculamos la proporción de varianza explicada y las proporciones acumuladas
prop_varianza <- pca$sdev^2 / sum(pca$sdev^2)
prop_varianza_acum <- cumsum(prop_varianza)

# Representamos la proporción de la varianza explicada
f<-ggplot(data = data.frame(prop_varianza, pc = 1:31),
aes(x = pc, y = prop_varianza)) +
  geom_col(width = 0.3, color='skyblue', fill = 'skyblue') +
  scale_y_continuous(limits = c(0,1)) +
  theme_bw()+
  labs(x = "Componentes principales",
       y = "Proporción de varianza explicada")

# Representamos la proporción de varianza explicada acumulada
g<-ggplot(data = data.frame(prop_varianza_acum, pc = 1:31),
       aes(x = pc, y = prop_varianza_acum, group = 1)) +
  geom_point(color='skyblue') +
  geom_line(color = 'skyblue') +
  theme_bw() +
  labs(x = "Número de componentes principales",
       y = "Proporción de varianza explicada acumulada")

ggsave("../figuras/varianza.png", 
       plot = f, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

ggsave("../figuras/varianza_acumulada.png", 
       plot = g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

ggsave("../figuras/pca_varianzas.png", 
       plot = f+g, 
       width = 15, 
       height = 5, 
       units = "in", 
       dpi = 300)

f+g
```

En la curva de proporción de varianza acumulada vemos que, a partir de la decimoquinta componente principal, la proporción de varianza que explica cada una de las siguientes componentes es casi imperceptible. Es por esto por lo que escogeremos solamente las 15 primeras componentes principales para entrenar otro modelo de Random Forest, con la intención de simplificar el modelo para evitar sobreajustarnos a los datos de entrenamiento.

```{r}
# Creamos el conjunto de entrenamiento formado por TODAS las componentes principales
datos_pca_train <- data.frame(
  etiqueta = datos_train$etiqueta, 
  pca$x
  ) 

# Calculamos el modelo
set.seed(42)
modelo_pca <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7 + PC8 + PC9 + PC10 + PC11 + PC12 + PC13 + PC14 + PC15,
  data = datos_pca_train,
  ntree = 100
)

# Calculamos el conjunto de validación
datos_pca_validacion <- predict(pca, newdata = datos_validation) 
datos_pca_validacion <- data.frame(
  etiqueta = datos_validation$etiqueta,
  ruta = datos_validation$ruta,
  datos_pca_validacion
) 

# Evaluar el modelo en el conjunto de validacion
predicciones_pca <- predict(modelo_pca, newdata = datos_pca_validacion)
confusionMatrix(predicciones_pca, datos_pca_validacion$etiqueta)
modelo_pca$importance
```

Mostramos las imágenes mal clasificadas:

```{r}
mal_clasificados <- datos_pca_validacion[predicciones_pca != datos_pca_validacion$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = '#a44')
}
```


### Test con nuestras fotos

Cargamos, leemos y etiquetamos los datos: 

```{r}
# Definir la ruta base
ruta_base_imagenes <- "imagenes_recortadas/test/Meteorológico"

# lectura de las imágenes de Nublado
ruta_nublado <- file.path(ruta_base_imagenes, "Nublado")
archivos_nublado <- list.files(
  path = ruta_nublado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado <- data.frame(
  ruta = archivos_nublado,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado <- file.path(ruta_base_imagenes, "Despejado")
archivos_despejado <- list.files(
  path = ruta_despejado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado <- data.frame(
  ruta = archivos_despejado,
  etiqueta = 1 # despejado
)

# lectura de las imágenes 
datos_test <- rbind(df_nublado, df_despejado)
lista_test <- leer_imagenes(datos_test)

# extracción de características
datos_test <- datos_test %>% 
  extraer_rgb(lista_test) %>% 
  extraer_hsv(lista_test) %>% 
  extraer_brillo_contraste(lista_test) %>% 
  extraer_ratio_azul(lista_test) %>% 
  extraer_sobel(lista_test) %>% 
  extraer_saturacion_bloques(lista_test) %>% 
  extraer_sd_V(lista_test) %>% 
  extraer_ratio_blanco(lista_test) %>% 
  extraer_ratio_gris(lista_test) %>% 
  extraer_ratio_sombras(lista_test)

lista_procesada_test <- mejorar_texturas(datos_test, lista_test)
datos_test <- extraer_sobel(datos_test, lista_procesada_test)

# Fusionamos los datos de train y validation para entrenar el modelo final con todos ellos
datos_entrenamiento <- rbind(datos_train, datos_validation)
```

Hacemos PCA:
```{r}
pca2 <- prcomp(
  ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste + Sobel_mean + Bloque_1_media_S + Bloque_2_media_S + Bloque_3_media_S + Bloque_4_media_S + Bloque_5_media_S + Bloque_6_media_S +Bloque_7_media_S +Bloque_8_media_S + Bloque_9_media_S +Bloque_10_media_S + Bloque_11_media_S + Bloque_12_media_S + Bloque_13_media_S + Bloque_14_media_S + Bloque_15_media_S + Bloque_16_media_S + Ratio_Blanco + Ratio_Gris + sd_V + Ratio_Sombras,
  data=datos_entrenamiento, 
  center=TRUE, 
  scale=TRUE
  )
```

```{r}
# Creamos el conjunto de entrenamiento con todas las componentes principales
datos_pca2_train <- data.frame(
  etiqueta = datos_entrenamiento$etiqueta, 
  pca2$x
  ) 
```

```{r}
# Calculamos la proporción de varianza explicada y las proporciones acumuladas
prop_varianza <- pca2$sdev^2 / sum(pca2$sdev^2)
prop_varianza_acum <- cumsum(prop_varianza)

# Representamos la proporción de varianza explicada acumulada
ggplot(data = data.frame(prop_varianza_acum, pc = 1:31),
       aes(x = pc, y = prop_varianza_acum, group = 1)) +
  geom_point(color='skyblue') +
  geom_line(color = 'skyblue') +
  theme_bw() +
  labs(x = "Número de componentes principales",
       y = "Proporción de varianza explicada acumulada")
```



```{r}
# Calculamos el modelo
modelo_pca2 <- randomForest(
  etiqueta ~  PC1 + PC2 + PC3 + PC4 + PC5 + PC6 + PC7 + PC8 + PC9 + PC10 + PC11 + PC12 + PC13 + PC14 + PC15,
  data = datos_pca2_train,
  ntree = 100
)

# Calculamos el conjunto de test
datos_pca_test <- predict(pca2, newdata = datos_test) 
datos_pca_test <- data.frame(
  etiqueta = datos_test$etiqueta,
  ruta = datos_test$ruta,
  datos_pca_test
) 

# Evaluar el modelo en el conjunto de prueba
predicciones_pca2 <- predict(modelo_pca2, newdata = datos_pca_test)
confusionMatrix(predicciones_pca2, datos_pca_test$etiqueta)
modelo_pca2$importance
```

```{r}
# Visualizamos los errores
mal_clasificados <- datos_pca_test[predicciones_pca2!= datos_pca_test$etiqueta, ]
par(mfrow=c(2,3), mar=c(0.1, 0.1, 0.1, 0.1))
for (i in 1:dim(mal_clasificados)[1]){
  plot(image_read(mal_clasificados[i, "ruta"]))
  if (mal_clasificados[i, "etiqueta"] == 0){aux <- "Nublado"} else{aux <- "Despejado"}
  legend("top", legend = paste0("Etiqueta real:\n", aux), bg = "transparent", bty = "n", text.col = '#a44')
}
```


# Interpretación del PCA 

```{r}
comp_ppales <- pca2$rotation

for(i in 1:15){
  cat("--- Componente principal",i,"---\n")
  var_imp <- names(comp_ppales[,i])[abs(comp_ppales[,i]/max(abs(comp_ppales[,i])))> 0.9]
  for(j in 1:length(var_imp)){
    cat(var_imp[j],"\n")
  }
  cat("\n")
}

```

En el código anterior hemos mostrado por pantalla las variables que más influyen en cada una de las quince componentes principales. Si nos fijamos en la primera, vemos que las variables más influyente de la primera componente principal (que, a su vez es la más influyente en el modelo según el criterio _MeanDecreaseGini_) son la saturación mediana y la saturación por bloques. Esto nos revela que nuestro modelo se guía principalmente por la intensidad de los colores de la imagen y el cambio de esta dependiendo del segmente de la imagen. La segunda componente está sobre todo influenciada por los valores medianos de V, B y G, es decir, por la luminosidad mediana de la imagen y los valores de azul y verde. La tercera componente se guía por el contraste de la imagen y la desviación estándar de V, mientras que la cuarta está más influenciada por el seno del canal H y la quinta, por el ratio de sombras.
