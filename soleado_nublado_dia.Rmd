---
title: "Clasificador metereológico"
output: html_document
date: "2025-12-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
rm(list = ls())
library(jpeg, quietly = T)
library(class, quietly = T)
library(caret, quietly = T)
library(randomForest, quietly = T)
library(magick, quietly = T)
library(tidyr, quietly = T)
library(plotly, quietly = T)
library(ggplot2, quietly = T)
# cargamos las funciones de lectura y extracción de características de imágenes
source("funciones_extraccion_caracteristicas.R")
```


# Lectura de las imágenes

Vamos a diferenciar los días despejados de aquellos nublados.
Para ello cargamos todas las imágenes destinadas a entrenamiento y reservaremos un 20% para validación.

```{r}
# Definir la ruta base
ruta_base_imagenes <- "imagenes_recortadas/train/Meteorológico"

# lectura de las imágenes de Nublado
ruta_nublado <- file.path(ruta_base_imagenes, "Nublado")
archivos_nublado <- list.files(
  path = ruta_nublado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado <- data.frame(
  ruta = archivos_nublado,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado <- file.path(ruta_base_imagenes, "Despejado")
archivos_despejado <- list.files(
  path = ruta_despejado,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado <- data.frame(
  ruta = archivos_despejado,
  etiqueta = 1 # despejado
)

# lectura de las imágenes 

datos_imagenes <- rbind(df_nublado, df_despejado)

set.seed(42) 

indices_entrenamiento <- createDataPartition(
  y = datos_imagenes$etiqueta,
  p = 0.8,
  list = FALSE 
)

# Crear los conjuntos de datos de TRAIN y VALIDATION
datos_train <- datos_imagenes[indices_entrenamiento, ]
datos_validation <- datos_imagenes[-indices_entrenamiento, ]
```

## Extracción y análisis de características

Para entrenar este nuevo clasificador, que tendrá que distinguir entre cielos nublados y despejados, hemos propuesto un total de 10 características: los valores de los tres canales RGB originales de la imagen, los valores de HSV; descomponiendo el matiz o _hue_ (H) en sus valores de seno y coseno (ya que al ser H un ángulo no es conveniente tratarlo igual que las variables S y V), el brillo, el contraste y el ratio de píxeles azules.

```{r}
# leemos las imágenes
lista_train <- leer_imagenes(datos_train)
lista_validation <- leer_imagenes(datos_validation)

# extraemos las medianas de R, G, y B 
datos_train <- extraer_rgb(datos_train, lista_train)
datos_validation <- extraer_rgb(datos_validation, lista_validation)

# extraemos las medianas del espacio HSV
datos_train <- extraer_hsv(datos_train, lista_train)
datos_validation <- extraer_hsv(datos_validation, lista_validation)

# extraemos las ratios de azul 
datos_train <- extraer_ratio_azul(datos_train, lista_train)
datos_validation <- extraer_ratio_azul(datos_validation, lista_validation)

# extraemos las medianas de brillo y contraste 
datos_train <- extraer_brillo_contraste(datos_train, lista_train)
datos_validation <- extraer_brillo_contraste(datos_validation, lista_validation)
```

```{r}
# Transformar datos a formato largo para representarlos 
datos_long <- pivot_longer(datos_train, cols = c("R_mediana", "G_mediana", "B_mediana"), 
                           names_to = "Feature", values_to = "Valor")

# Representamos los histogramas de las variables
ggplot(datos_long, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
ggplot(datos_long, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  labs(title = "Separación de clases usando las medianas de los canales RGB",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
```

Al representar los histogramas de los valores medianos del espacio RGB, vemos que en los 3 casos podríamos considerar que los datos siguen una distribución normal. Si bien es llamativa la alta concentración del valor 0 en el canal R, esto podría deberse simplemente a la ausencia de este color en imágenes del cielo despejado y nublado. También es destacable que, como era de esperar, el canal con valores medianos más altos es el B y también es este canal el que presenta una diferencia entre la distribución de los datos según las clases más apreciable. Una vez comprobada la distribución que siguen nuestros datos, al realizar un diagrama de cajas podemos observar que, efectivamente, el canal B es el que proporciona una distinción mayor entre clases, aunque esta diste de ser completa. 

```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~R_mediana,
  y = ~G_mediana,
  z = ~B_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'R Mediana'),
    yaxis = list(title = 'G Mediana'),
    zaxis = list(title = 'B Mediana')
  ),
  title = 'Distribución de imágenes por valor mediano de los canales RGB'
)
fig_hsv
```
Representando conjuntamente las tres variables en el espacio, podemos observar que, aunque ambas clases se agrupen consistentemente, la frontera de decisión no está claramente definida.


```{r}
# Transformar datos a formato largo para representarlos 
datos_long_hsv <- pivot_longer(
  datos_train, 
  cols = c("H_cos_mediana", "H_sin_mediana", "S_mediana", "V_mediana"), 
  names_to = "Feature", 
  values_to = "Valor"
  )

# Representamos los histogramas de las variables
ggplot(datos_long_hsv, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  facet_wrap(~ Feature, scales = "fixed") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
ggplot(datos_long_hsv, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  labs(title = "Separación de clases usando HSV",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
```
Al representar los histogramas de los 4 valores extraídos de la representación de las imágenes en el espacio HSV, podemos observar que todas, excepto $\cos H$ parecen presentar una distribución normal.
En el diagrama de cajas podemos observar que la mayor distancia entre clases la presenta el canal S.


```{r}
# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_cos_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'cos(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv

# Representación en el espacio 
fig_hsv <- plot_ly(
  data = datos_train,
  x = ~H_sin_mediana,
  y = ~S_mediana,
  z = ~V_mediana,
  color = ~as.factor(etiqueta),
  colors = c('gray', 'skyblue'),
  type = 'scatter3d',
  mode = 'markers'
)
fig_hsv <- fig_hsv %>% layout(
  scene = list(
    xaxis = list(title = 'sin(H) Mediana'),
    yaxis = list(title = 'S Mediana'),
    zaxis = list(title = 'V Mediana')
  ),
  title = 'Distribución parcial de las imágenes por valor mediano de los canales HSV'
)
fig_hsv
```

Si representamos en el espacio las variables $\cos H$, S,V y después las variables $\sin H$, S,V observaremos que el coseno de la variable H junto con los canales S y V parecen distinguir mejor entre clases que la representación que sustituye el coseno por el seno.


```{r}
ggplot(datos_train, aes(x = H_cos_mediana, y = H_sin_mediana, color = etiqueta)) +
  geom_point(size = 3, alpha = 0.7) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  theme_minimal() +
  labs(title = "Relación entre el coseno y el seno del canal H",
       x = "cos(H)",
       y = "sin(H)",
       color = "Clase")
```


```{r}
# Transformar datos a formato largo para representarlos 
datos_long_bcr <- pivot_longer(
  datos_train, 
  cols = c( "Ratio_Azul", "Brillo", "Contraste"), 
  names_to = "Feature", values_to = "Valor"
  )

# Representamos los histogramas de las variables
ggplot(datos_long_bcr, aes(x = Valor, fill = etiqueta)) +
  geom_histogram(position = "identity", alpha = 0.6, bins = 30) +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  facet_wrap(~ Feature, scales = "free") +
  theme_minimal() +
  labs(title = "Histogramas por canal y clase",
       x = "Valor",
       y = "Frecuencia",
       fill = "Clase")

# Representamos el diagrama de cajas
ggplot(datos_train, aes(x = Brillo, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  labs(title = "Separación de clases usando el brillo",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal() 
```
Como podemos ver en los histogramas, la variable brillo presenta una marcada distribución normal, mientras que las variables contraste y ratio de azul distan mucho de hacerlo, sobre todo el ratio de azul. Es por esto que solo hemos representado el brillo en un diagrama de cajas, que nos demuestra que la variable brillo no es capaz de distinguir claramente entre clases, ya que las medianas de ambas poblaciones están muy próximas y el IQR es bastante similar.

## Aplicación de PCA y Selección de Modelo



```{r}
# Entrenar el modelo Random Forest con características RGB
modelo_rf_rgb <- randomForest(
  etiqueta ~ R_mediana + G_mediana + B_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_rgb <- predict(modelo_rf_rgb, newdata = datos_validation)
confusionMatrix(predicciones_rgb, datos_validation$etiqueta)
```

```{r}
mal_clasificados <- datos_validation[predicciones_rgb != datos_validation$etiqueta, ]
rutas_mal_clasificados <- mal_clasificados$ruta
for (i in 1:length(rutas_mal_clasificados)){
  plot(image_read(rutas_mal_clasificados[i]))
}
```



Entrenamos y evaluamos el modelo con las características HSV:
```{r}
# Entrenar el modelo Random Forest con características HSV
modelo_rf_hsv <- randomForest(
  etiqueta ~ H_cos_mediana + H_sin_mediana + S_mediana + V_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_hsv <- predict(modelo_rf_hsv, newdata = datos_validation)
confusionMatrix(predicciones_hsv, datos_validation$etiqueta)
```
Vemos cuáles ha clasificado mal:
```{r}
mal_clasificados <- datos_validation[predicciones_hsv != datos_validation$etiqueta, ]
ruta_mal_clasificados <- mal_clasificados$ruta 
for (ruta in ruta_mal_clasificados){
  plot(as.raster(lista_validation[[ruta]]))
}
```

Entrenamos y evaluamos el modelo con las características RGB+HSV
```{r}
# Entrenar el modelo Random Forest con características combinadas
modelo_rf_completo <- randomForest(
  etiqueta ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_completo <- predict(modelo_rf_completo, newdata = datos_validation)
confusionMatrix(predicciones_completo, datos_validation$etiqueta)
modelo_rf_completo$importance
```


# todo un poco desordenado:

```{r}
# Entrenar el modelo Random Forest con características RGB
modelo_rf_rgb <- randomForest(
  etiqueta ~ R_mediana + G_mediana + B_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_rgb <- predict(modelo_rf_rgb, newdata = datos_validation)
confusionMatrix(predicciones_rgb, datos_validation$etiqueta)
```

```{r}
mal_clasificados <- datos_validation[predicciones_rgb != datos_validation$etiqueta, ]
rutas_mal_clasificados <- mal_clasificados$ruta
for (i in 1:length(rutas_mal_clasificados)){
  plot(image_read(rutas_mal_clasificados[i]))
}
```


```{r}
# Probar a extraer características del espacio HSV
datos_train <- extraer_hsv(datos_train, lista_train)
datos_validation <- extraer_hsv(datos_validation, lista_validation)
```

Entrenamos y evaluamos el modelo con las caracteristicas HSV
```{r}
# Entrenar el modelo Random Forest con características HSV
modelo_rf_hsv <- randomForest(
  etiqueta ~ H_cos_mediana + H_sin_mediana + S_mediana + V_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_hsv <- predict(modelo_rf_hsv, newdata = datos_validation)
confusionMatrix(predicciones_hsv, datos_validation$etiqueta)
```
Vemos cuáles ha clasificado mal:
```{r}
mal_clasificados <- datos_validation[predicciones_hsv != datos_validation$etiqueta, ]
ruta_mal_clasificados <- mal_clasificados$ruta 
for (ruta in ruta_mal_clasificados){
  plot(as.raster(lista_validation[[ruta]]))
}
```

Entrenamos y evaluamos el modelo con las caracteristicas RGB+HSV
```{r}
# Entrenar el modelo Random Forest con características combinadas
modelo_rf_completo <- randomForest(
  etiqueta ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_completo <- predict(modelo_rf_completo, newdata = datos_validation)
confusionMatrix(predicciones_completo, datos_validation$etiqueta)
modelo_rf_completo$importance
```


Representamos las caracterísiticas:

```{r}
library(ggplot2)
library(tidyr)
```


```{r}

# Transformar datos a formato largo para plotear 
datos_long <- pivot_longer(datos_train, cols = c("R_mediana", "G_mediana", "B_mediana"), 
                           names_to = "Feature", values_to = "Valor")

ggplot(datos_long, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  labs(title = "Separación de Clases usando RGB",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
```


```{r}
# Transformar datos a formato largo para plotear 
datos_long_hsv <- pivot_longer(datos_train, cols = c("H_cos_mediana", "H_sin_mediana", "S_mediana", "V_mediana"), 
                           names_to = "Feature", values_to = "Valor")

ggplot(datos_long_hsv, aes(x = Feature, y = Valor, fill = etiqueta)) +
  geom_boxplot() +
  scale_fill_manual(values = c("grey", "skyblue"), labels = c("Nublado", "Soleado")) +
  labs(title = "Separación de Clases usando RGB",
       y = "Intensidad Mediana (0-1)") +
  theme_minimal()
```


```{r}
# Extraer característica ratio azul para los conjuntos de entrenamiento y prueba
datos_train <- extraer_ratio_azul(datos_train, lista_train)
datos_validation <- extraer_ratio_azul(datos_validation, lista_validation)
# Entrenar el modelo Random Forest con características RGB
modelo_ratio_azul <- randomForest(
  etiqueta ~ Ratio_Azul,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_ratio_azul <- predict(modelo_ratio_azul, newdata = datos_validation)
confusionMatrix(predicciones_ratio_azul, datos_validation$etiqueta)
```

Probamos un modelo con características S y ratio de azul:

```{r}
# Entrenar el modelo Random Forest con características RGB
modelo_s_azul <- randomForest(
  etiqueta ~ S_mediana + Ratio_Azul,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_s_azul <- predict(modelo_s_azul, newdata = datos_validation)
confusionMatrix(predicciones_s_azul, datos_validation$etiqueta)
```

```{r}
modelo_mas_completo <- randomForest(
  etiqueta ~ R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul,
  data = datos_train,
  ntree = 100,
)
# Evaluar el modelo en el conjunto de prueba
predicciones_mas_completas <- predict(modelo_mas_completo, newdata = datos_validation)
confusionMatrix(predicciones_mas_completas, datos_validation$etiqueta)
modelo_mas_completo$importance
```
Probamos a incluir al modelo más completo las características brillo y contraste:


```{r}
# Probamos más características
datos_train <- extraer_brillo_contraste(datos_train, lista_train)
datos_validation <- extraer_brillo_contraste(datos_validation, lista_validation)
```


```{r}
set.seed(42)
modelo_bc <- randomForest(
  etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_bc <- predict(modelo_bc, newdata = datos_validation)
confusionMatrix(predicciones_bc, datos_validation$etiqueta)
modelo_bc$importance
```




```{r}
set.seed(42)
modelo_3_mejores <- randomForest(
  etiqueta ~  S_mediana + Ratio_Azul + Contraste,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_3_mejores <- predict(modelo_3_mejores, newdata = datos_validation)
confusionMatrix(predicciones_3_mejores, datos_validation$etiqueta)
modelo_3_mejores$importance
```




### test con nuestras fotos

Cargamos, leemos y etiquetamos los datos: 

```{r}
# Definir la ruta base
ruta_base_imagenes_validation <- "Soleado_Nublado"

# lectura de las imagenes de Nublado
ruta_nublado_validation <- file.path(ruta_base_imagenes_validation, "Nublado")
archivos_nublado_validation <- list.files(
  path = ruta_nublado_validation,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_nublado_validation <- data.frame(
  ruta = archivos_nublado_validation,
  etiqueta = 0 # nublado
)


# Leer y etiquetar imágenes del subdirectorio Despejado

ruta_despejado_validation <- file.path(ruta_base_imagenes_validation, "Despejado")
archivos_despejado_validation <- list.files(
  path = ruta_despejado_validation,
  pattern = "\\.jpe?g$",
  full.names = TRUE,
  recursive = FALSE
)

df_despejado_validation <- data.frame(
  ruta = archivos_despejado_validation,
  etiqueta = 1 # despejado
)

# lectura de las imagenes 
datos_imagenes_validation <- rbind(df_nublado_validation, df_despejado_validation)

# leemos las imágenes y les pasamos un filtro
lista <- leer_imagenes(datos_imagenes_validation)

SoloCieloVersionada <- function(df, lista, corte, girar=FALSE, ploteamos = FALSE){
  
  # creamos lista vacía donde almacenaremos las imagenes filtradas
  lista2 <- list()
  
  # definimos el kernel
  k <- matrix(c(-1,-1,-1,-1,8,-1,-1,-1,-1), ncol = 3, nrow = 3)
  
  for(ruta in df$ruta){
    img <- lista[[ruta]]
    img <- image_scale(img,"501x501!")
    # aplicamos el filtro
    imgf <- img %>% image_convolve(kernel = k)
    # pasamos a escala de grises
    imgf <- imgf %>% image_quantize(colorspace = 'gray') %>% image_data(channels = 'gray')
    imgf <- as.numeric(imgf)
    
    # sacamos los colores de la imagen
    rgb_im <- image_data(img, channels = "rgb")
    
    # rotamos si ha sido especificado
    if(girar){
      imgf<- aperm(imgf,c(2,1,3))
      rgb_im <- aperm(rgb_im, c(2,3,1))
    } else{
      rgb_im <- aperm(rgb_im,c(3,2,1))
    }
    
    rgb_num <- as.integer(rgb_im)/255
    dim(rgb_num) <- dim(rgb_im)
    rgb_final <- rgb_num
    
    # tapar con negro partes no identificadas como cielo
    for(i in seq(51, 501, by=50)){
      for(j in seq(51, 501, by=50)){
          a <- sum(as.numeric(imgf[(j-50):j,(i-50):i,1]))
        if(a>corte){
          rgb_final[(j-50):nrow(rgb_num),(i-50):i,] <- 0 
        }
        # Intentar volver a mostrar trozos de cielo que igual 
        # tienen algo que no es cielo arriba
        b <- sum(rgb_num[(j-50):j,(i-50):i,3])
        if(b>1300&a<0.8*corte){
          rgb_final[(j-50):nrow(rgb_num),(i-50):i,] <- rgb_num[(j-50):nrow(rgb_num),(i-50):i,]
        }
      }
    }
    
    # sacamos por pantalla el resultado si así se pide
    if(ploteamos){plot(as.raster(rgb_final)) }
    
    # añadimos la imagen filtrada a la lista
    lista2[[ruta]] <- rgb_final
    
  }
  
  # devolvemos la lista con las imágenes filtradas
  return(lista2)

}

# extracción de caracterísitcas
datos_imagenes_validation <- extraer_rgb(datos_imagenes_validation, lista)
datos_imagenes_validation <- extraer_hsv(datos_imagenes_validation, lista)
datos_imagenes_validation <- extraer_ratio_azul(datos_imagenes_validation, lista)
```


```{r}
SoloCieloVersionada <- function(df, lista, corte, girar=FALSE, ploteamos = FALSE){
  
  # creamos lista vacía donde almacenaremos las imagenes filtradas
  lista2 <- list()
  
  # definimos el kernel
  k <- matrix(c(-1,-1,-1,-1,8,-1,-1,-1,-1), ncol = 3, nrow = 3)
  
  for(ruta in df$ruta){
    img <- image_read(ruta)
    img <- image_scale(img,"501x501!")
    # aplicamos el filtro
    imgf <- img %>% image_convolve(kernel = k)
    # pasamos a escala de grises
    imgf <- imgf %>% image_quantize(colorspace = 'gray') %>% image_data(channels = 'gray')
    imgf <- as.numeric(imgf)
    
    # sacamos los colores de la imagen
    rgb_im <- image_data(img, channels = "rgb")
    
    # rotamos si ha sido especificado
    if(girar){
      imgf<- aperm(imgf,c(2,1,3))
      rgb_im <- aperm(rgb_im, c(2,3,1))
    } else{
      rgb_im <- aperm(rgb_im,c(3,2,1))
    }
    
    rgb_num <- as.integer(rgb_im)/255
    dim(rgb_num) <- dim(rgb_im)
    rgb_final <- rgb_num
    
    # tapar con negro partes no identificadas como cielo
    for(i in seq(51, 501, by=50)){
      for(j in seq(51, 501, by=50)){
          a <- sum(as.numeric(imgf[(j-50):j,(i-50):i,1]))
        if(a>corte){
          rgb_final[(j-50):nrow(rgb_num),(i-50):i,] <- 0 
        }
        # Intentar volver a mostrar trozos de cielo que igual 
        # tienen algo que no es cielo arriba
        b <- sum(rgb_num[(j-50):j,(i-50):i,3])
        if(b>1300&a<0.8*corte){
          rgb_final[(j-50):nrow(rgb_num),(i-50):i,] <- rgb_num[(j-50):nrow(rgb_num),(i-50):i,]
        }
      }
    }
    
    # sacamos por pantalla el resultado si así se pide
    if(ploteamos){plot(as.raster(rgb_final)) }
    
    # añadimos la imagen filtrada a la lista
    lista2[[ruta]] <- rgb_final
    
  }
  
  # devolvemos la lista con las imágenes filtradas
  return(lista2)

}
```


```{r}
# filtramos imágenes y extraemos características de estas 
lista_filtro <- SoloCieloVersionada(datos_imagenes_validation, lista, 130)
datos_imagenes_filtradas <- data.frame(
  ruta = datos_imagenes_validation$ruta,
  etiqueta = datos_imagenes_validation$etiqueta
  )

datos_imagenes_filtradas <- extraer_rgb(datos_imagenes_filtradas, lista_filtro)
datos_imagenes_filtradas <- extraer_hsv(datos_imagenes_filtradas, lista_filtro)
datos_imagenes_filtradas <- extraer_ratio_azul(datos_imagenes_filtradas, lista_filtro)
```

```{r}
predicciones_mas_completas <- predict(modelo_mas_completo, newdata = datos_imagenes_filtradas)
confusionMatrix(predicciones_mas_completas, datos_imagenes_filtradas$etiqueta)
mal_clasificadas <- datos_imagenes_filtradas[predicciones_mas_completas != datos_imagenes_filtradas$etiqueta, ]
for (i in 1:length(mal_clasificadas)){plot(as.raster(lista_filtro[[mal_clasificadas[i,]$ruta]]))}
mal_clasificadas
```


Aplicamos el modelo:

```{r}
predicciones_mas_completas <- predict(modelo_mas_completo, newdata = datos_imagenes_validation)
confusionMatrix(predicciones_mas_completas, datos_imagenes_validation$etiqueta)
modelo_mas_completo$importance
mal_clasificadas <- datos_imagenes_validation[predicciones_hsv != datos_imagenes_validation$etiqueta, ]
rutas_mal_clasificadas <- mal_clasificadas$ruta
```




Probamos solo con el S

```{r}
modelo_s <- randomForest(
  etiqueta ~  S_mediana ,
  data = datos_train,
  ntree = 100
)
# Evaluar el modelo en el conjunto de prueba
predicciones_s <- predict(modelo_s, newdata = datos_validation)
confusionMatrix(predicciones_s, datos_validation$etiqueta)
modelo_s$importance
```


<!-- Modelo a guardar (usa todo el train): -->
<!-- ```{r} -->
<!-- set.seed(42) -->
<!-- rf_despejado_nublado <- randomForest( -->
<!--   etiqueta ~  R_mediana + G_mediana + B_mediana + H_cos_mediana + H_sin_mediana + S_mediana + V_mediana + Ratio_Azul + Brillo + Contraste, -->
<!--   data = rbind(datos_train,datos_validation), -->
<!--   ntree = 100, -->
<!--   importance = TRUE -->
<!-- ) -->
<!-- importance(rf_despejado_nublado) -->

<!-- save(rf_despejado_nublado,file="datosR/rf_despejado_nublado.RData") -->
<!-- ``` -->
<!-- ```{r} -->
<!-- set.seed(42) -->
<!-- rf_despejado_nublado3 <- randomForest( -->
<!--   etiqueta ~  S_mediana + Ratio_Azul + Contraste, -->
<!--   data = rbind(datos_train,datos_validation), -->
<!--   ntree = 100, -->
<!--   importance = TRUE -->
<!-- ) -->
<!-- importance(rf_despejado_nublado3) -->

<!-- save(rf_despejado_nublado3, file="datosR/rf_despejado_nublado3.RData") -->
<!-- ``` -->

